<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base configuration: HTTPS only by default -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug configuration allows user-installed certs for development tools -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>

    <!-- Development: cleartext for emulator localhost access -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>

    <!-- Production: HTTPS with certificate pinning placeholder -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.rasoiai.com</domain>
        <pin-set expiration="2027-06-01">
            <!-- Replace with actual certificate pins before production release -->
            <pin digest="SHA-256">PLACEHOLDER_PIN_REPLACE_BEFORE_PRODUCTION</pin>
            <!-- Backup pin from CA -->
            <pin digest="SHA-256">PLACEHOLDER_BACKUP_PIN</pin>
        </pin-set>
    </domain-config>
</network-security-config>
