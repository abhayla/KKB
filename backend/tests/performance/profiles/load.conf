# Average Load Test — simulate expected daily traffic
# Purpose: Validate performance under normal conditions
# Duration: 10 minutes, ramp to 20 users over 2 minutes
# Cost: ~10-15 Gemini API calls ($0.03)
# Target: p95 < 15s (meal gen), p95 < 500ms (CRUD), error rate < 1%

locustfile = tests/performance/locustfile.py
headless = true
users = 20
spawn-rate = 1
run-time = 10m
host = http://localhost:8000
html = tests/performance/reports/load_report.html
csv = tests/performance/reports/load
