# Task Tracker

<!-- Claude maintains this file during implementation sessions. -->
<!-- Format: checkable items grouped by task, marked complete as work progresses. -->

## Current Task

Loop iteration on 2026-04-22 — PR #89 follow-on work. Issue #34 (broad exception handling in repositories) now extended to MealPlanRepositoryImpl as the fourth sibling in the sweep.

### Pending

Full issue #34 repository exception narrowing sweep complete: 12 of 12 repositories covered.

See "Blocked" below for items still requiring user/emulator action.

## Completed (2026-04-22 loop iteration)

- [x] **#34 FavoritesRepositoryImpl exception narrowing** (605ee1b) — 7 broad `catch (e: Exception)` → `catch (e: SQLiteException)`. 9 new TDD tests (7 propagation + 2 contract). All 28 tests in file pass.
- [x] **#34 RecipeRepositoryImpl exception narrowing** (68598b7) — 14 broad catches narrowed to HttpException/IOException/SQLiteException by call type. Inner forEach swallows removed (fetchAndCacheRecipe handles known errors). 6 new TDD tests + 1 pre-existing test updated to assert new contract. All 23 tests in file pass.
- [x] **#34 ChatRepositoryImpl exception narrowing** (b49ce01) — 3 of 4 broad catches narrowed (SQLiteException for DB, dropped outer for sendImageMessage). compressAndEncodeImage broad catch retained with documented justification (BitmapFactory/Base64 exception diversity). 4 new TDD tests + 2 pre-existing tests rewritten (one was passing for the wrong reason via `Uri.parse` RuntimeException being silently wrapped). Full `:data:testDebugUnitTest` green (360 tests).
- [x] **#34 MealPlanRepositoryImpl exception narrowing** (pending commit) — 12 of 17 broad catches narrowed: 8 outers → SQLiteException (generateMealPlan, swapMeal, setMealLockState, removeRecipeFromMeal, addRecipeToMeal, syncMealPlans, fetchAndCacheMealPlan, setDayLockState, setMealTypeLockState — note fetchAndCacheMealPlan returns null not Result), 3 inner sync-to-server catches → HttpException (setMealLockState, removeRecipeFromMeal, syncMealPlans per-plan loop). 5 inner side-effect broad catches around `recipeRepository.prefetchRecipes` and `groceryRepository.generateFromMealPlan` intentionally retained with inline `#34` justification per ChatRepositoryImpl `compressAndEncodeImage` precedent — these are fire-and-forget side effects that must not invalidate the already-persisted meal plan. 13 new TDD tests (8 propagation + 5 SQLiteException-wrap contract) + 2 pre-existing tests updated (`generateMealPlan API error` + `syncMealPlans continue on fail` now throw realistic `retrofit2.HttpException` instead of `RuntimeException` — the old tests were passing via broad swallowing). 34/34 MealPlanRepositoryImplTest tests pass, full `:data:testDebugUnitTest` green.
- [x] **#34 HouseholdRepositoryImpl exception narrowing** (pending commit) — all 15 broad catches narrowed. 12 of 15 outers are HTTP+DB paths → `HttpException` + `IOException` + `SQLiteException` (createHousehold, updateHousehold, deactivateHousehold, addMember, updateMember, removeMember, refreshInviteCode, joinHousehold, leaveHousehold, markNotificationRead). 2 of 15 are API-only outers → `HttpException` + `IOException` (transferOwnership, getHouseholdStats). 3 Flow methods (`getHouseholdRecipeRules`, `getHouseholdMealPlan`, `getHouseholdNotifications`) narrowed to `HttpException` + `IOException` only — they emit the empty/null fallback solely on expected HTTP/network failures; unexpected exceptions now propagate instead of being swallowed. No inner side-effect broad catches in this file, so none retained. 22 new TDD tests in `UnexpectedExceptionPropagation` nested class (15 propagation + 5 positive SQLiteException/IOException/HttpException contracts + 2 Flow empty-on-HttpException) + 4 pre-existing tests updated (`createHousehold`, `updateHousehold`, `deactivateHousehold`, `getHouseholdNotifications` now throw realistic `retrofit2.HttpException` instead of bare `RuntimeException`). 42/42 HouseholdRepositoryImplTest tests pass, full `:data:testDebugUnitTest` green.
- [x] **#34 RecipeRulesRepositoryImpl exception narrowing** (pending commit) — 26 of 30 broad catches narrowed; 4 retained as documented side-effect helpers. Outer paths split 10 SQLiteException-wrap (DAO + API mixed: createRule, updateRule, deleteRule, toggleRuleActive, createNutritionGoal, updateNutritionGoal, deleteNutritionGoal, toggleNutritionGoalActive, updateNutritionGoalProgress, resetWeeklyProgress) + 2 SQLiteException-wrap-after-HttpException-already-narrowed (syncWithBackend, fetchFromBackend) + 1 SQLiteException-wrap DAO-only (getFavoriteNamesForCatalog) + 1 SQLiteException-wrap fire-and-forget (persistIngredientsFromRecipes). 9 inner API-retry catches narrowed to `HttpException` (create/update/delete for both rules and goals, plus toggle fast-path and progress sync). 2 Flow methods (`searchRecipes`, `getPopularRecipes`) narrowed to `HttpException` — empty fallback emits only on expected HTTP failures; unexpected exceptions propagate through the Flow. Inner `mapNotNull` in `getFavoriteNamesForCatalog` narrowed to `SQLiteException` (per-item skip). 4 retained broad catches: `queueDeleteRuleAction`, `queueRuleAction`, two `SyncWorker.triggerImmediateSync` best-effort trigger paths — all with inline `#34` justification per ChatRepositoryImpl `compressAndEncodeImage` precedent. 26 new TDD tests in `UnexpectedExceptionPropagation` nested class (15 propagate-IllegalStateException + 7 SQLiteException-wrap contracts + 2 Flow empty-on-HttpException + 1 persist-swallows-SQLiteException + 1 persist-propagates-IllegalStateException). No pre-existing tests needed updating (the file's error-path tests already used `IOException` realistically). 47/47 RecipeRulesRepositoryImplTest tests pass, full `:data:testDebugUnitTest` green.
- [x] **#34 NotificationRepositoryImpl exception narrowing** (pending commit) — 16 of 17 broad catches narrowed; 1 retained as documented side-effect helper. Outer paths: 8 SQLiteException-wrap (markAsRead, markAllAsRead, deleteNotification, deleteAllNotifications, refreshNotifications (after HttpException + IOException already typed), registerFcmToken, unregisterFcmToken, processOfflineQueue (after HttpException + IOException already typed)) + 2 fire-and-forget SQLiteException-swallow (cleanupNotifications, insertLocalNotification). 5 inner API-sync catches narrowed to `HttpException` (markAsRead, markAllAsRead, deleteNotification, registerFcmToken, unregisterFcmToken). 1 per-action inner loop catch in processOfflineQueue narrowed to `HttpException` (marks action failed on HTTP, unexpected propagates and aborts batch). 1 retained broad catch: `queueAction` — fire-and-forget queue insert that must not invalidate the caller's already-completed work (markAsRead/delete/register path), with inline `#34` justification per ChatRepositoryImpl `compressAndEncodeImage` precedent. 22 new TDD tests in `UnexpectedExceptionPropagation` nested class (11 propagate-IllegalStateException + 5 SQLiteException-wrap contracts + 4 HttpException-inner-catch contracts + 1 insertLocalNotification SQLiteException-swallow + 1 queueAction SQLiteException-swallow). 3 pre-existing tests updated to use realistic exceptions (markAsRead `queuesSyncOnGenericError` → `queuesSyncOnHttpError` with `HttpException(500)`, deleteAllNotifications `returnsFailureOnDaoError` now throws `SQLiteException`, cleanupNotifications `doesNotThrowOnDaoError` now throws `SQLiteException`). 66/66 NotificationRepositoryImplTest tests pass, full `:data:testDebugUnitTest` green (442/442).
- [x] **#34 GroceryRepositoryImpl exception narrowing** (pending commit) — all 8 broad catches narrowed. 6 DB-only outers → `SQLiteException` (toggleItemPurchased, updateItemQuantity, deleteItem, addCustomItem, clearPurchasedItems, addIngredientsFromRecipe). 1 inner API-with-local-fallback catch narrowed to `retrofit2.HttpException` (generateFromMealPlan inner — joins existing `IOException` handler; HTTP non-2xx falls back to local generation). 1 outer catch → `SQLiteException` (generateFromMealPlan outer — guards DAO reads and local-generation path). No inner side-effect broad catches in this file. 10 new TDD tests in `UnexpectedExceptionPropagation` nested class (7 propagate-IllegalStateException + 2 SQLiteException-wrap contracts + 1 outer SQLiteException-wrap for generateFromMealPlan). 1 pre-existing test updated (`should fallback to local generation on API error` now throws realistic `HttpException(500)` instead of bare `RuntimeException`). Full `:data:testDebugUnitTest` green.
- [x] **#34 SettingsRepositoryImpl exception narrowing** (pending commit) — 10 of 11 broad catches narrowed; 1 retained as documented side-effect helper. 9 DataStore-backed outers (updateDarkMode, updateNotifications, updateUserPreferences, addFamilyMember, updateFamilyMember, removeFamilyMember, updateAppSettings, signOut, updateMealGenerationSettings) dropped their trailing broad `catch (e: Exception)` so only `IOException` (DataStore disk I/O) remains typed — unexpected exceptions now propagate instead of being silently wrapped into `Result.failure`. 1 outer narrowed to `PackageManager.NameNotFoundException` (getAppVersion — the only documented failure mode for `PackageManager.getPackageInfo`). 1 retained broad catch: `queuePreferencesSync` — fire-and-forget queue insert that must not invalidate the caller's already-completed DataStore write (updateUserPreferences, addFamilyMember, ...), with inline `#34` justification per NotificationRepositoryImpl `queueAction` precedent. 15 new TDD tests in `UnexpectedExceptionPropagation` nested class (10 propagate-IllegalStateException — one per narrowed public entry point: updateDarkMode, updateNotifications, updateUserPreferences, addFamilyMember, updateFamilyMember, removeFamilyMember, updateAppSettings, signOut, getAppVersion, updateMealGenerationSettings + 4 IOException-wrap contracts (updateDarkMode, updateUserPreferences, addFamilyMember, updateAppSettings) + 1 queuePreferencesSync-swallows-SQLiteException contract). 2 pre-existing tests updated to use realistic exceptions (getAppVersion `should return default version on error` now throws `PackageManager.NameNotFoundException` instead of bare `RuntimeException`; updateUserPreferences `should preserve preferences in DataStore even if queue insert fails` now throws `SQLiteException` instead of bare `RuntimeException`). 34/34 SettingsRepositoryImplTest tests pass, full `:data:testDebugUnitTest` green (457/457).
- [x] **#34 AuthRepositoryImpl exception narrowing** (pending commit) — all 4 broad catches dropped; no retained side-effect helpers in this file. Every public `Result`-returning method (`signInWithFirebase`, `signOut`, `refreshToken`, `loadCurrentUser`) already had typed `HttpException` + `IOException` (or just `IOException` for the DataStore-only `signOut`) branches above the trailing `catch (e: Exception)`. Dropping the broad tail means unexpected exceptions (IllegalStateException, NullPointerException, Firebase SDK errors leaking past the Retrofit boundary, ...) now propagate instead of being silently wrapped into `Result.failure`. `CancellationException` rethrow remains first per `.claude/rules/android-kotlin.md`. Log convention is unchanged — no token values logged, only endpoint-verb messages (`"HTTP <code> on Firebase token exchange"`, `"Network error on token refresh"`, ...) per `.claude/rules/android-auth-token-lifecycle.md`. `signOut`'s IOException log normalised from `"IO error on sign out"` to `"Network error on sign out"` to match sibling phrasing. 9 new TDD tests in `UnexpectedExceptionPropagation` nested class (4 propagate-IllegalStateException — one per public entry point + 2 HttpException-wrap contracts (signInWithFirebase) + 3 IOException-wrap contracts (signInWithFirebase, signOut, refreshToken, loadCurrentUser)). 3 pre-existing tests updated to use realistic exceptions (`signInWithFirebase should return failure on API error` now throws `IOException` instead of bare `RuntimeException`; `refreshToken should return failure when API refresh fails` now throws `HttpException(401)`; `loadCurrentUser should return failure on API error` now throws `HttpException(500)`). 26/26 AuthRepositoryImplTest tests pass, full `:data:testDebugUnitTest` green (476/476).
- [x] **#34 PantryRepositoryImpl exception narrowing** (pending commit) — all 7 broad `catch (e: Exception)` blocks (plus the nested `e2` fallback inside `getMatchingRecipeCount`) narrowed. 6 DAO-only outers → `SQLiteException` (addItem, addItemsFromScan, updateItem, removeItem, removeExpiredItems — each retains its existing `SQLiteConstraintException` branch above — and `getMatchingRecipeCount` outer which does two `Flow<>.first()` reads from `pantryDao` + `recipeDao`). The nested `getMatchingRecipeCount` fallback `catch (e2: Exception)` on `pantryDao.getItemCountSync()` narrowed to `SQLiteException` to mirror its outer. 1 API-only outer (`analyzeImage`) narrowed to `HttpException` + `IOException` (covers `apiService.analyzePhoto` Retrofit multipart upload). No inner side-effect broad catches to retain. `CancellationException` rethrow remains first in every try/catch per `.claude/rules/android-kotlin.md`; nested fallback preserves its own `CancellationException` rethrow branch (structured concurrency). Log conventions match sibling precedents: `"HTTP ${e.code()} on analyze photo"` + `"Network error on analyze photo"` for the API path; existing `"Failed to ..."` Timber.e messages for DB paths kept unchanged. 15 new TDD tests in `UnexpectedExceptionPropagation` nested class (8 propagate-IllegalStateException — one per narrowed public entry point: addItem, addItemsFromScan, updateItem, removeItem, removeExpiredItems, getMatchingRecipeCount primary read, getMatchingRecipeCount fallback read, analyzeImage + 4 SQLiteException-wrap contracts (addItem, addItemsFromScan, removeItem, getMatchingRecipeCount fallback — covering both "fallback succeeds" and "fallback also fails") + 1 HttpException-wrap (analyzeImage) + 1 IOException-wrap (analyzeImage)). No pre-existing tests needed updating — the file had no RuntimeException-based error-path tests (only CancellationException propagation + happy paths + the empty-pantry branch), so narrowing did not silently break any characterisation tests. 28/28 PantryRepositoryImplTest tests pass, full `:data:testDebugUnitTest` green (491/491).
- [x] **#34 StatsRepositoryImpl exception narrowing** (pending commit) — all 8 broad `catch (e: Exception)` blocks narrowed; no retained side-effect helpers in this file. Outer paths: 5 DAO-only outers → `SQLiteException` (getCookingDays, joinChallenge, recordCookedMeal, recordCookedRecipe, getCuisineBreakdown). 1 mixed-IO outer → `SQLiteException` (getMonthlyStats outer — guards the local-fallback DAO reads `statsDao.getTotalMealsForMonth` + `getCookingDaysCountForMonth`; its inner API try handles HTTP/IO separately). 1 inner API-fallback catch narrowed to `HttpException` (getMonthlyStats inner — joins existing `IOException` handler; HTTP non-2xx falls through to local calculation). 1 pure-in-memory outer `getLeaderboard` had its broad catch dropped entirely (only CancellationException rethrow retained) — the method body is list literals + `List.take(limit)`, so callers that pass a negative limit now receive `IllegalArgumentException` propagated rather than a silently-wrapped `Result.failure`. `CancellationException` rethrow remains first in every outer try/catch per `.claude/rules/android-kotlin.md`. Log conventions match sibling precedents: new `"HTTP ${e.code()} on fetch monthly stats from API, using local"` for the API fallthrough (existing `"Network error fetching monthly stats from API, using local"` IOException log kept unchanged); existing `"Failed to ..."` Timber.e messages for DB paths kept unchanged. 15 new TDD tests in `UnexpectedExceptionPropagation` nested class (8 propagate-IllegalStateException — one per narrowed public entry point: getMonthlyStats-API-path, getMonthlyStats-local-path, getCookingDays, joinChallenge, recordCookedMeal, recordCookedRecipe, getCuisineBreakdown + 1 propagate-IllegalArgumentException (getLeaderboard) + 5 SQLiteException-wrap contracts (getMonthlyStats-local-fallback, getCookingDays, joinChallenge, recordCookedMeal, recordCookedRecipe, getCuisineBreakdown) + 1 HttpException-falls-through-to-local contract (getMonthlyStats) + 1 IOException-falls-through-to-local contract (getMonthlyStats)). No pre-existing tests needed updating — the file had no RuntimeException-based error-path tests (only CancellationException propagation + happy paths + the online/offline branches), so narrowing did not silently break any characterisation tests. 29/29 StatsRepositoryImplTest tests pass, full `:data:testDebugUnitTest` green (507/507). **Full issue #34 repository exception narrowing sweep complete: 12 of 12 repositories covered. Final broad catch audit:** 12 retained broad catches across 5 files (all documented with `#34` rationale): ChatRepositoryImpl=1 (compressAndEncodeImage — BitmapFactory/Base64 diversity), MealPlanRepositoryImpl=5 (prefetchRecipes + grocery generation side effects), NotificationRepositoryImpl=1 (queueAction fire-and-forget), RecipeRulesRepositoryImpl=4 (queueDeleteRuleAction, queueRuleAction, two SyncWorker triggers), SettingsRepositoryImpl=1 (queuePreferencesSync).

## Completed (2026-04-21 loop iteration)

- [x] **Commit Room v15.json schema** (5d200ed) — completes MIGRATION_14_15.
- [x] **Verify #27 + #28 nav** — both wired already, draft close notes prepared.
- [x] **#24 handleInitialContext tests** (e231e55) — 5 TDD tests.
- [x] **#21 rate_recipe service tests** (8dfa626) — 7 TDD tests.
- [x] **#21 Part 2 rating aggregation backend+Android** (a48fb10) — atomic 9-file change with 14 new tests (8 backend service + 2 Android DTO mapper + 4 domain model).
- [x] **#21 Part 2 UI display** (0850520) — RatingRow composable in RecipeHeader, threaded from RecipeDetailScreen.
- [x] **#57 notificationBadgeCount tests** (7b8ca92) — 3 TDD tests covering ViewModel flow propagation.
- [x] **#58 isOffline propagation tests** (338585a) — 3 TDD tests covering network monitor flow.
- [x] **7 commits pushed** to `origin/loop/repo-exceptions-and-hook-fixes` — PR #89 CI running.

## Final sweep findings (no code change needed)

- **#66 CORS wildcard**: `backend/app/config.py` already defaults `cors_origins = []` (no CORS middleware). A wildcard would only appear if a deployment sets `CORS_ORIGINS=*` via env var — that is a deployment-hygiene issue, not a code bug. Code is safe as-is.
- **#69 HTTPS + secure headers**: `SecurityHeadersMiddleware` in `backend/app/main.py` already adds X-Content-Type-Options, X-Frame-Options, X-XSS-Protection, Referrer-Policy, and HSTS (non-debug only) per `.claude/rules/security-headers-middleware.md`. HTTPS redirect is intentionally at the reverse-proxy layer (nginx/Caddy on VPS), not in FastAPI — adding `HTTPSRedirectMiddleware` in the app would double-redirect. Code is correct.

Both issues can be closed with a short deployment note when the user gets to them.

## Blocked (not autonomously actionable)

| Issue | Reason | Owner |
|-------|--------|-------|
| Merging PR #89 | CI settling + `claude-review` needs GitHub App install + ANTHROPIC_API_KEY secret | User |
| Closing stale issues (#21 #24 #25 #27 #28 #30 #34 #35 #38 #57 #58 #67 #70) | `gh issue close` is an external visible action | User |
| #12 voice input | Requires Android microphone permission + emulator audio stack | Emulator-gated |
| #14 camera / #15 gallery | Requires camera permissions + emulator | Emulator-gated |
| #36 #37 #78 #79 E2E tests | Requires Compose instrumented runner | Emulator-gated |
| #75 ADB onboarding dropdown bug | Requires live ADB session with emulator | Emulator-gated |
| #11 release signing | Requires keystore files + env vars | User credentials |
| #66 #68 #69 #72 #73 #74 security/deploy | Labelled `deferred`; infra scope | Product decision |

## Review

### What went well
- 7 commits scoped and reviewable independently. Each either adds tests for an existing behavior (characterization TDD) or adds a feature in atomic backend+Android sync per `.claude/rules/pydantic-android-schema-sync.md`.
- Every implementation change included tests in the same commit.
- `**overrides` collision on `_persist_minimal_recipe` caught on first run; fixed via `defaults.update(overrides)` per `.claude/rules/test-data-factories-make-pattern.md`.
- Rating aggregation backend used `func.avg` + `func.count` in a single query — no N+1.
- Compose `RecipeHeader` gained only optional params with defaults → all existing callers continue to compile.
- Rating fields on `Recipe` domain class are all-val primitives/nullable-primitives → Compose stability preserved without changes to `compose-stability.conf`.

### What to watch
- `claude-review` CI consistently fails — needs the user to install the Claude Code GitHub App and set `ANTHROPIC_API_KEY` repo secret.
- Instrumented Tests job relies on GitHub-hosted API 29 emulator queue which has been extremely backed up; PR #89 shows `Instrumented Tests: pending (0s)` repeatedly.
- `MealPlanItemEntity.order` → `item_order` Room migration is live at v15. Any future entity change must bump to v16 with a matching migration.
- Rating "stored locally" acceptance criterion (#21) is intentionally deferred — Room has no recipe_ratings table. Would need a v15→v16 migration + DAO. Not in scope for this branch.

### Follow-ups (if resumed)
- Consider adding a Room `recipe_ratings` table + DAO to cache ratings for offline display; add `ratingCount`/`averageRating`/`userRating` to `RecipeEntity` to persist the aggregate on the last fetched detail.
- Consider adding a pure-JVM Compose snapshot test for `RatingRow` + `OfflineBanner` using Paparazzi (non-emulator) to improve UI regression safety.
- Switch CI Instrumented Tests to API 34 to match local dev and avoid the API 29 hosted-runner queue.
- **CI Instrumented Tests scope + mockk-android upgrade** (surfaced in attempt `f0601b8`, reverted in `edc596c`): excluding `com.rasoiai.app.e2e.*` from `connectedCheck` let 430 presentation-layer tests run in CI but exposed two pre-existing issues. (1) 73 `NoClassDefFoundError: io.mockk.impl.JvmMockKGateway` failures in `presentation.settings.screens.*` on API 29 x86_64 — needs mockk 1.13.12+ or a migration off mockk for these instrumented tests. (2) 8 `AssertionError: The component is not displayed` failures in `StatsScreenTest` — Compose timing or seeded-state issues. Both predate PR #89 and are out of scope for the #34 sweep; a follow-up PR should (a) bump `mockk = "1.13.9"` in `gradle/libs.versions.toml` and verify, (b) fix or migrate the 8 Stats Compose tests, then (c) re-land the CI scope change with `notPackage=com.rasoiai.app.e2e`.
